import 'package:flutter/material.dart';

class NearByCharietiesView extends StatelessWidget {
  const NearByCharietiesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('NearByCharietiesView'),
      ),
    );
  }
}
