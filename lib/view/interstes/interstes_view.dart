import 'package:flutter/material.dart';

class InterstesView extends StatelessWidget {
  const InterstesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('InterstesView'),
      ),
    );
  }
}
