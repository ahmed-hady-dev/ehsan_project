import 'package:flutter/material.dart';

class CharityView extends StatelessWidget {
  const CharityView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CharityView'),
      ),
    );
  }
}
