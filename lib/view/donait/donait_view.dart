import 'package:flutter/material.dart';

class DonaitView extends StatelessWidget {
  const DonaitView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DonaitView'),
      ),
    );
  }
}
