import 'package:flutter/material.dart';

class DonaitingRequestView extends StatelessWidget {
  const DonaitingRequestView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DonaitingRequestView'),
      ),
    );
  }
}
