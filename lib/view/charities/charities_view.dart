import 'package:flutter/material.dart';

class CharitiesView extends StatelessWidget {
  const CharitiesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CharitiesView'),
      ),
    );
  }
}
